# Async / Await — תרגילים

---

## רמה בסיסית

---

### תרגיל 5 — המרת Promise ל-async/await

המר את קוד ה-Promise הבא ל-`async/await`:

</div>

```javascript
fetch("https://jsonplaceholder.typicode.com/todos/1")
  .then(res => res.json())
  .then(data => console.log(data));
```

---

<div dir="rtl">

### תרגיל 6 — פונקציה async שמחזירה "Done"

כתוב פונקציה `async` שמחזירה `"Done"`.

---

### תרגיל 7 — ריבוע עם await

כתוב פונקציה `async` שמקבלת מספר ומחזירה את **הריבוע** שלו (עם `await`).

---

## רמה בינונית

---

### תרגיל 9 — async שמביאה משתמשים

כתוב פונקציה `async` שמביאה את המשתמשים מתרגיל 8 ומדפיסה אותם.

---

### תרגיל 11 — async עם try/catch

כתוב פונקציה `async` שמשתמשת בבדיקת הזוגיות מתרגיל 10 ומדפיסה הודעה בהתאם.

---

### תרגיל 13 — המרת שרשרת Promises ל-async/await

המר את שרשרת ה-Promises מתרגיל 12 ל-`async/await`.

---

### תרגיל 14 — 2 קריאות API ברצף

כתוב פונקציה `async` שמבצעת **2 קריאות API ברצף** (אחת אחרי השנייה).

---

## רמה מתקדמת

---

### תרגיל 15 — 3 קריאות API במקביל עם Promise.all

כתוב פונקציה שמבצעת **3 קריאות API במקביל** עם `Promise.all`.

---

### תרגיל 16 — Promise.all עם טיפול בשגיאות

שפר את תרגיל 15 כך שאם אחת הקריאות נכשלת — תטפל בשגיאה בצורה נכונה.

---

### תרגיל 18 — loop עם await — קריאות ברצף

כתוב פונקציה `async` שעוברת על מערך של URLs ומביאה את כולם (**עם `await` בתוך loop**).

---

### תרגיל 19 — קריאות URLs במקביל

שפר את תרגיל 18 כך שהקריאות יהיו **במקביל** (ולא אחת אחרי השנייה).

---

### תרגיל 20 — async/await לשרשרת המורכבת

השתמש ב-`async/await` כדי לחבר את `getUser`, `getOrders` ו-`getOrderDetails` ולהדפיס את כל פרטי ההזמנות.

**דרישות:**
- השתמש ב-`async/await`
- השתמש ב-`try/catch` לטיפול בשגיאות
- הדפס את כל פרטי ההזמנות

</div>
המערכת מציגה את async_await_תרגילים.md.